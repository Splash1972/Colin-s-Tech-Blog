<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Colin's Tech Blog</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>

  <div class="wrapper">
    <header>
      <nav class="navbar fixed-top" style="background-color: #acacac;">
        <div class="container-fluid">
          <h1>Colin's Tech Blog</h1>
          <button class="btn btn-outline-light ml-auto"><a href="/">Home</a></button>
          {{#if logged_in}}
          <button class="btn btn-outline-light ml-auto"><a href="/dashboard">Dashboard</a></button>
          <button id="logout" class="btn btn-outline-danger ml-auto">Logout</button>
          {{else}}
          <button class="btn btn-outline-light ml-auto" onclick="location.href='/login'">Login</button>
          {{/if}}
        </div>
  </div>
  </nav>

  </header>
  <br>
  <br>
  <main>
    <div class="bg">
      {{{body}}}
    </div>
  </main>


  {{#if logged_in}}
  {{!--
  <script id="edit-blog-template" type="text/x-handlebars-template"> --}}
    {{!-- {{> editBlog}} --}}
{{!-- </script> --}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>